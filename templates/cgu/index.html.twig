{% extends 'base.html.twig' %}

{# {% block title %}Hello CguController!{% endblock %} #}

{% block body %}
    <div class="max-w-7xl mx-auto py-28 relative">
        <h1 class="text-2xl md:text-3xl font-bold text-sky-800 mt-4 text-center">{{ cguPage.mainTitle }}</h1>

        <div class="mt-8">
            {{ cguPage.content|raw }}
        </div>
    </div>
{% endblock %}
